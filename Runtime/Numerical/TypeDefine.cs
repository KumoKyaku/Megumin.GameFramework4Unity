using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Megumin.GameFramework.Numerical
{
    //不要定义主属性，由项目自己绝对
    //public class MainNumericalType
    //{
        
    //}

    /// <summary>
    /// 仅供参考的子属性
    /// </summary>
    public class SubNumericalType
    {
        public const string 基础值 = nameof(基础值);

        public const string 装备固定加成 = nameof(装备固定加成);
        public const string 装备系数加成 = nameof(装备系数加成);
        public const string 装备加成后总计 = nameof(装备加成后总计);

        public const string Buff固定加成 = nameof(Buff固定加成);
        public const string Buff系数加成 = nameof(Buff系数加成);
        public const string Buff加成后总计 = nameof(Buff加成后总计);

        public const string 属性关联固定加成 = nameof(属性关联固定加成);
        public const string 属性关联系数加成 = nameof(属性关联系数加成);
        public const string 属性关联后总计 = nameof(属性关联后总计);

        public const string 后期固定加成 = nameof(后期固定加成);
        public const string 后期系数加成 = nameof(后期系数加成);
        public const string 面板值 = nameof(面板值);
    }
}
